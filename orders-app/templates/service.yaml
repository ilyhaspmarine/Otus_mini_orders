apiVersion: v1
kind: Service
metadata:
  name: {{ include "orders-app.serviceName" . | quote }}
  labels:
    app: {{ include "orders-app.appName" . | quote }}
spec:
  selector:
    app: {{ include "orders-app.appName" . | quote }}
  ports:
    - name: {{ .Values.service.portName }}
      protocol: TCP
      port: {{ int .Values.service.port }}
      targetPort: 8000
  type: {{ .Values.service.type }}
